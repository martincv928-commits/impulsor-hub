[pytest]
testpaths = tests
pythonpath = .
markers =
    real_godot: requires a real Godot executable on PATH/IMPULSOR_HUB_GODOT_PATH. Auto-skipped (not failed) when unavailable; run `pytest -m real_godot` to select only these.
