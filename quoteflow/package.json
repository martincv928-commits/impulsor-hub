{
  "name": "quoteflow",
  "version": "0.1.0",
  "private": true,
  "description": "QuoteFlow V0.1: cotizaciones por voz o texto, parser local, PDF y compartir.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "npx --yes serve -l 5173 ."
  }
}
